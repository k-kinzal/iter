queue shell {
  enqueue = "true"
  dequeue = "true"
}

workspace local {
  base = "."
}

agent claude {
  mode = print
  command = "claude"
}

runner {
  agent = claude
  workspace = local
  queue = shell
  continue_on_error = false
  behavior = wait
  prompt = "do the thing"
}
