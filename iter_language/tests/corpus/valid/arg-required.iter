arg worktree_name

queue memory

workspace local {
  base = "."
}

agent claude {
  mode = interactive
  command = "claude"
}

runner {
  agent = claude
  workspace = local
  queue = memory
  continue_on_error = false
  behavior = wait
  prompt = "Working on {{arg.worktree_name}}."
}
