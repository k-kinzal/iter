queue memory

workspace local {
  base = "."
}

agent generic {
  command = ["python", "./agent.py"]
  env {
    API_KEY = "sk-test"
    DEBUG   = "1"
  }
}

runner {
  continue_on_error = false
  behavior = wait
}

prompt "Generic env test."
