queue memory
workspace local { base = "." }

agent router {
  strategy = bogus

  primary {
    kind    = claude
    mode    = print
    command = "claude"
  }
}

runner { continue_on_error = false  behavior = wait }
prompt "test"
