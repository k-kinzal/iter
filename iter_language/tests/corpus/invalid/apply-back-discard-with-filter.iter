queue memory

workspace clone {
  base = "."
  excludes = []
  preserve_mtime = true
  apply_back {
    mode = discard
    excludes = ["*.md"]
  }
}

agent claude {
  mode = print
  command = "claude"
}
